constante pinSom: inteiro = 5
constante BREATH_DELAY: inteiro = 1

declare i: inteiro

comando_setup
    ative(som, pinSom)
fim_comando_setup

comando_loop
	ligar(som, pinSom, i)
	esperar(BREATH_DELAY)
	desligar(som, pinSom)
	esperar(10)
fim_comando_loop
